	int count = 0;     //计数
	for (int i = 0; i < 32; ++i)   //控制循环的次数
	{
		if (x % 2 == 1)
		{
			count += 1;
		}
		x >>= 1;      //右移一位再赋值给x继续循环
	}
	return count;
}
int main()
{
	printf("%d\n", CountBitNum(4));
	printf("%d\n", CountBitNum(0xFFFFFFFF));
	return 0;
}
