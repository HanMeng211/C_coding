#include<stdio.h>
int main()
{
	int arr[40];
	int i = 0;
	int n;
	int j;
	scanf_s("%d\n", &n);
	for (i = 0; i < n;i++)
	{
		scanf_s("%d", &arr[i]);
	}
	for (i = 0; i < n; i++)
	{
		for (j = i; j < n; j++)
		{
			if (arr[i] > arr[j])
			{
				int tem = arr[i];
				arr[i] = arr[j];
				arr[j] = tem;
			}
		}
	}
	for (i = 0; i < 5; i++)
		printf("%d\n", arr[i]);
	return 0;
}
