#define  _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
int main()
{
	int money,sum,empty;
	scanf("%d", &money);
	int buy = money * 1;
	sum = buy;
	empty = buy;
	while (empty > 1)
	{
		sum += empty / 2;//喝的总瓶数
		empty = empty / 2 + empty % 2;//空瓶书
	}
	printf("%d\n", sum);
	return 0;
}
