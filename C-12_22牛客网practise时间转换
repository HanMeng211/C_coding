#include<stdio.h>
int main()
{
    int seconds;
    scanf("%d",&seconds);
    int h = seconds / 3600;
    int m = (seconds - h * 3600) / 60;
    int s = (seconds - 3600 * h) - (60 * m);
    printf("%d %d %d\n",h,m,s);
    return 0;
}
