#include<stdio.h>
#include<math.h>
double GetV(double r)
{
	double π = 3.1415926;
	double V = ((4.0 / 3) * π) * pow(r, 3);
	return V;
}
int main()
{
	double r;
	scanf("%lf", &r);
	printf("%.3lf\n", GetV(r));
	return 0;
}
