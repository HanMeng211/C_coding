#include<stdio.h>
int main()
{
	int num;
	while (scanf_s("%d", &num) != EOF)
	{
		int a = num;
		for (int i = 0; i < num; i++)
		{
			for (int j = a; j > 0; j--)
			{
				printf("* ");
			}
			printf("\n");
			a--;
		}
	}
	return 0;
}
