#include<stdio.h>
#include<math.h>
float GetBmi(int w, int h)
{
	float BMI = w / pow((h * 0.01), 2.0);
	return BMI;
}
int main()
{
	int wight, hight;
	scanf("%d %d", &wight, &hight);
	printf("%.2f\n", GetBmi(wight, hight));
	return 0;
}
