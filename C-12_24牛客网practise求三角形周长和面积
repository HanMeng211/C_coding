#include<stdio.h>
#include<math.h>
int main()
{
	int a, b, c;
	scanf_s("%d %d %d", &a, &b, &c);
	float l = (float)a + (float)b + (float)c;
	float p = l / 2;
	float s = sqrt(p * (p - a) * (p - b) * (p - c));
	printf("circumference=%.2f area=%.2f", l, s);
	return 0;
}
