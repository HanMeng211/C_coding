#include<stdio.h>
int LeapYear(int x)
{
	if (x / 4 == 0 && x / 100 != 0 && x / 400 == 0)
	{
		printf("%d年是闰年", x);
	}
	printf("%d年不是闰年", x);
	return 0;
}
int main()
{
	int a;
	scanf_s("%d", &a);
	LeapYear(a);
	printf("\n");
	return 0;
}
