#include<stdio.h>
int main()
{
	int a, b, c, d;
	scanf_s("%d %d %d %d", &a, &b, &c, &d);
	int max1 = a > b ? a : b;
	int max2 = c > d ? c : d;
	int max = max1 > max2 ? max1 : max2;
	printf("%d\n", max);
	return 0;
}
