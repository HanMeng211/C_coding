#include<stdio.h>
int main()
{
    int achievement;
    while(scanf("%d",&achievement) != EOF)
    {
        if(achievement >= 60)
        {
            printf("Pass\n");
        }
        else
        {
            printf("Fail\n");
        }
    }
    return 0;
}
