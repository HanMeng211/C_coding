#include<stdio.h>
int main()
{
	int i, j, c;
	for (i = 1; i <= 9; i++)
	{
		for (j = 1; j <= i; j++)
		{
			c = i * j;
			printf("%d*%d=%2d ", i, j, c);   //%2d表示固定输出两位
		}
		printf("\n");
	}
	return 0;
}
